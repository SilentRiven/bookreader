# bookreader 开发规划

以 legado 代码为起点的独立 Android 开源阅读器项目。本文档记录目标、约束、阶段规划与刻意砍掉的范围。

## 一、项目定位

- **形态**：开源 Android 小说阅读器，**起点为 legado 代码，与上游完全解离，作为独立项目维护**
- **用户**：自己日常使用 + 开源社区
- **内容来源**：用户自配书源（沿用 legado 机制，规避版权风险）
- **同步方案**：使用现成 WebDAV 服务器（家里本地化部署 Nextcloud / Alist 等），不自建同步服务端

## 二、当前局势盘点

| 维度 | 现状 |
|---|---|
| 开发者背景 | Java 后端高级 / Android 客户端 0 经验 / 业余时间 1 人 |
| 时间预期 | 不设硬性 deadline，按兴趣推进 |
| 技术栈 | Kotlin（与起点代码一致，现代 Android 主流） |
| 工具链 | JDK 21（已与上游 JDK 17 解耦） |
| 仓库 | `git@github.com:SilentRiven/bookreader.git`（独立项目，不跟上游 sync） |

## 三、关键认知与裁剪原则

1. **起点代码已覆盖最初功能列表 90%**（多格式解析、TTS、自定义书源、换源、阅读统计、字典、本章说、净化排版等）。本项目的工作不是"实现这些"，而是"挑不满意的少数几项去改"。
2. **前 2-3 个月不会有可见输出**：Android 零基础 + Kotlin 学习 + 起点代码阅读，先打底。
3. **4 个想动的方向（阅读界面 / 书架管理 / 同步后端 / AI 增强）不同时做**，按风险与可达性排序推进。
4. **自建同步后端已收敛为"装一个 WebDAV 服务"**，从开发任务变为运维任务，工作量从月级降到小时级。**不要回头自己写同步服务端**——那是个人开源项目的死亡陷阱。
5. **独立项目身份**：可以自由重构、改包名、改 JDK 版本、改 CI 流水线，不再受"upstream 怎么做我也要怎么做"的约束。

## 四、分阶段规划

### 阶段 0：环境与认知（约 1-2 个月业余时间）

**目标**：能读懂 legado，能本地构建运行。

- Kotlin 速通：2-4 周覆盖语法、协程、扩展函数、空安全（Java 高级背景可加速）
- Android 基础：Activity/Fragment 生命周期、Jetpack（ViewModel / Flow / Room）、Material Components
- legado 本地构建：clone → 签 debug build → 装到自己手机用
- 源码地图通读，重点模块：
  - `io.legado.app.model`：书籍/章节/书源数据模型
  - `io.legado.app.help`：全局工具与逻辑
  - `io.legado.app.ui.book.read`：阅读核心（**最难，先看懂别动**）

### 阶段 1：快速胜利 —— WebDAV 同步打通（1-2 周）

**目标**：让"我的 fork 能在家里局域网同步书架"成为现实，获得正反馈。

- 在家里设备装 Nextcloud / Alist / hacdias/webdav 任一
- legado 客户端配置 WebDAV → 验证书架 / 进度 / 书签同步
- 几乎零代码改动

### 阶段 2：低风险改造 —— 书架与管理（1-2 个月）

**目标**：第一个能展示"我的差异化"的 fork。

- 书架卡片样式调整
- 自定义分类标签（如"已完结 / 追更中"）
- 批量管理 UI 优化
- 导入流程优化

> 这块是数据驱动 + 列表 UI，对 Java 后端经验最友好，改坏了也不会影响阅读核心。

### 阶段 3：阅读体验改造（3-6 个月，慎重）

**前置条件**：已有 3-4 个月 Android 实战。

- **安全区**：新主题包、字体系统、阅读统计面板美化、设置项 UI 重做
- **危险区**：`ReadView` / `PageDelegate` 自绘与翻页动画——没有充分理解前不要动
- **禁区**：仿真翻页（legado 已有，不要重写）

### 阶段 4：AI / 特色增强（按兴趣，可选）

- 推荐做：AI 段落总结 / 摘要 / 解读
  - 后端写反向代理保护 API Key（可选模型：OpenAI / Claude / 通义 / 智谱）
  - 客户端调用代理
  - Java 后端经验直接派上用场
- **不要碰**：本章说 / 书评社区——需要账号体系 + 内容审核 + 长期运维，对个人开源项目是死亡之地

## 五、明确砍掉 / 推迟的范围

- ❌ 自建同步后端服务（已选 WebDAV，**不要回头**）
- ❌ 自己实现仿真翻页（legado 已有）
- ❌ 从零实现 TTS / 字典 / 净化 / 换源 / 阅读统计（legado 已有，复用即可）
- ❌ 跨端 iOS / Web（Android 都没拿下，先别想）
- ❌ 用户社区 / 本章说 / 书评（运维成本远超个人开源项目承受范围）

## 六、第一个里程碑

**3 个月内**：

- legado fork 能跑在自己手机上
- 分支带个性化标识
- 书架界面有可见的视觉 / 交互差异
- WebDAV 同步打通到家里服务器

这个目标具体、可达、有正反馈，比"做一个完整阅读器"切实得多。
